
TODO:
    - Plan structure in header files
    - Make line-search its own function (given some initial step size)
    - Change distance calculations to take enum: D_VALUE, D_GRAD, D_HESS that gives full control over which quatities are calculated


PhysicsEngine:
    - State
    - Params
    - Integrator

Integrator:
    - StateParams
    - CollisionManager
    virtual step()

    // Explicit methods (e.g. ForwardsEuler, RK4) can each just extend directly from Integrator

    // Implicit methods (e.g. BackwardsEuler, Trapazoidal, IKR4) need their own subclass so that they can implement their variational objective functions and send to optimizer
    ImplicitIntegrator
        - EnergyCalculator
        - Optimizer
        virtual value()
        virtual grad()
        virtual hess()
    
Optimizer:
    virtual solve(ImplicitIntegrator* integrator)

    // Solver specific parameters
    LFBGS(int m)
    Newton()





    
